task :tests do
  exec('pwd')
  exec('rspec') # this file will allow you to run multiple commands, so if you had process that requires multiple commands each time you can create a task with rake that will run all of these commands.
end
